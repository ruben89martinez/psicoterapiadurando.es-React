// src/theme.js
import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#3C8CE7',
    },
    secondary: {
      main: '#6C63FF',
    },
    // ... más colores
  },
})

export default theme
